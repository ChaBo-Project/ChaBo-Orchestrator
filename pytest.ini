[pytest]
# Put the src folder on the import path so tests can import the app modules
# (e.g. from components.guardrails.llm_guard import decide_safe).
pythonpath = src
# Run tests from CI test suite here. 
testpaths = tests/ci

# NOTE: The older ad hoc test scripts are not pytest-style and are excluded (for now). We can integrate later as required (TBD).
